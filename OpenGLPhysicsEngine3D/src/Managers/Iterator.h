#pragma once

// TODO: maybe next time maybe delete it
template<typename T, typename Iterable>
class Iterator
{
public:
	Iterator(Iterable ) {
		
	}

	virtual T* next() {

	}

private:
	bool hasNext();
};

